# Built binary
/kestrelynx
# `go build ./experiments/runtime-discovery` (no -o given) drops its binary
# here, named after its package directory.
/runtime-discovery
/out/
/site/
/.venv/

# Runtime discovery measurement output (never commit collected observations)
/experiments/runtime-discovery/out/

# Python documentation hooks
__pycache__/

# Local config (never commit real webhook URLs)
config.yml

# Editor / OS
*.swp
.DS_Store

# Internal design / planning notes (kept local, not published)
/design/
