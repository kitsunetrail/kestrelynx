# ServiceAccount the KestreLynx Deployment runs as; bound only to the
# read-only ClusterRole in clusterrole.yaml.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: kestrelynx
  namespace: kestrelynx
