{
  "case_id": "R5",
  "image": "kl-r5",
  "gt_b_scope": [
    "cryptography"
  ],
  "expected": [
    {
      "package": "libc6",
      "class": "os",
      "expected_usage": "used",
      "expected_verdict": "confirmed",
      "rationale": "linked by the python3.12 interpreter, compiled from source under /usr/local"
    },
    {
      "package": "pip",
      "class": "lang",
      "expected_usage": "not_used",
      "expected_verdict": "unobserved",
      "rationale": "lang-pkgs Finding: Trivy's PkgPath names a dist-info METADATA file, and the first stage has no way to relate that to a mapped object, so no lang-pkgs package can be confirmed whether it is used or not",
      "expected_factor": "lang_pkg_unmappable"
    },
    {
      "package": "setuptools",
      "class": "lang",
      "expected_usage": "not_used",
      "expected_verdict": "unobserved",
      "rationale": "lang-pkgs Finding: Trivy's PkgPath names a dist-info METADATA file, and the first stage has no way to relate that to a mapped object, so no lang-pkgs package can be confirmed whether it is used or not",
      "expected_factor": "lang_pkg_unmappable"
    },
    {
      "package": "wheel",
      "class": "lang",
      "expected_usage": "not_used",
      "expected_verdict": "unobserved",
      "rationale": "lang-pkgs Finding: Trivy's PkgPath names a dist-info METADATA file, and the first stage has no way to relate that to a mapped object, so no lang-pkgs package can be confirmed whether it is used or not",
      "expected_factor": "lang_pkg_unmappable"
    },
    {
      "package": "cryptography",
      "class": "lang",
      "expected_usage": "used",
      "expected_verdict": "unobserved",
      "expected_factor": "lang_pkg_unmappable",
      "rationale": "the server imports cryptography and its _rust.abi3.so is mapped by the running interpreter, so the package really is in use; the expected verdict is still unobserved because the first stage does not match lang-pkgs PkgPath against maps paths. The two fields differ here on purpose: recording this as unused would turn a matcher limitation into a claim about the workload"
    }
  ]
}
