{
  "case_id": "R8",
  "image": "kl-r8",
  "gt_b_scope": [
    "libsqlite3-0"
  ],
  "expected": [
    {
      "package": "libc6",
      "class": "os",
      "expected_usage": "used",
      "expected_verdict": "confirmed",
      "rationale": "every one of the three processes is dynamically linked against glibc"
    },
    {
      "package": "libsqlite3-0",
      "class": "os",
      "expected_usage": "used",
      "expected_verdict": "unobserved",
      "rationale": "two of the three processes hold a copy of this package's library open, but each copy lives at a path under /opt that the package database does not own, so an owned+matched path is not expected. The library itself is genuinely loaded, which is why the declared usage and the expected verdict differ"
    }
  ]
}
